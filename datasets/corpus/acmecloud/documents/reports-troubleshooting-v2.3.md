# 报表导出故障排查

出现 `permission denied` 时，依次确认用户权限、导出范围和租户套餐。

## 排查顺序

第一步检查用户是否拥有 `report.export`。第二步判断是否选择了跨租户范围；跨租户需要 `report.export.all`。第三步检查当前套餐是否支持该范围。

## 管理员仍然失败

管理员默认只能导出当前租户。如果管理员尝试跨租户导出但没有额外权限，仍会收到 `permission denied`。平台状态异常时通常会返回 5xx，而不是权限错误。
