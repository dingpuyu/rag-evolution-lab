# SSO 配置

AcmeCloud 2.3 将单点登录入口迁移到“身份中心 → 企业登录”。支持 SAML 2.0 和 OIDC。

## 前置条件

启用 SSO 需要企业版套餐，并且当前用户必须是已验证域名的组织管理员。

## 回调地址

所有 SSO 回调地址必须使用 HTTPS。系统会拒绝 HTTP 地址，包括测试环境。

## 生效方式

保存配置后先对一个测试账号启用。验证成功后再设置为全员强制登录。
