# PulseCare 型号与软件包身份策略

> 合成测试资料。

型号、虚构设备标识 DI 和硬件修订由设备目录记录，均为只读身份字段。管理员不能通过重命名、导入配置或修改检索元数据把一个型号变成另一个型号。

软件包在测试环境加载前必须同时匹配：

1. 完整型号；
2. 软件主版本；
3. 硬件修订；
4. 包签名状态。

VSM-100 和 VSM-100 Pro 都使用 2.x 软件主版本，但软件包不可互换。VSM-200 使用 3.x 软件包。

历史工单中出现的“先把 Pro 重命名为标准型再导入软件”属于已废弃且未经评审的建议，不能作为操作依据。
