# PulseCare VSM-100 软件 2.4 操作界面参考

> 历史合成资料，仅用于验证版本过滤；软件 2.4 已被 2.6 替代。

## 网络入口

在 2.4 中，有线网络入口为：`系统设置 → 通信 → 有线网络`。该版本没有“维护中心”入口，也不包含无线网络页面。

## 诊断日志

诊断日志从 `系统设置 → 维护 → 日志导出` 创建，导出文件使用 `.vlog` 后缀，单个包上限为 128 MB。

## 版本边界

本页只适用于型号 VSM-100 和软件 2.4。VSM-100 Pro、VSM-200 以及软件 2.6 的菜单名称不同。
