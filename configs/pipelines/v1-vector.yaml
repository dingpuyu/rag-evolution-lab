name: v1-vector
description: Naive vector RAG with fixed-size chunks.
query_processor:
  metadata_filter: false
  rewrite: false
retrieval:
  keyword:
    enabled: false
  vector:
    enabled: true
    top_k: 8
    distance: cosine
  fusion:
    enabled: false
rerank:
  enabled: false
context:
  top_k: 5
  token_budget: 4000
generation:
  citations_required: true
  refuse_without_context: true
